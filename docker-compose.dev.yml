services:
    app:
        restart: always
        build:
            context: ./
            dockerfile: ./Dockerfile.dev
        ports:
            - 3000:3000
        volumes:
            - ./:/app
            - /app/node_modules
